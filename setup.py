# Generated by Medikit 0.7.4 on 2020-01-02.
# All changes will be overriden.
# Edit Projectfile and run “make update” (or “medikit update”) to regenerate.

from codecs import open
from os import path

from setuptools import find_packages, setup

here = path.abspath(path.dirname(__file__))

# Py3 compatibility hacks, borrowed from IPython.
try:
    execfile
except NameError:

    def execfile(fname, globs, locs=None):
        locs = locs or globs
        exec(compile(open(fname).read(), fname, "exec"), globs, locs)


# Get the long description from the README file
try:
    with open(path.join(here, "README.rst"), encoding="utf-8") as f:
        long_description = f.read()
except:
    long_description = ""

# Get the classifiers from the classifiers file
tolines = lambda c: list(filter(None, map(lambda s: s.strip(), c.split("\n"))))
try:
    with open(path.join(here, "classifiers.txt"), encoding="utf-8") as f:
        classifiers = tolines(f.read())
except:
    classifiers = []

version_ns = {}
try:
    execfile(path.join(here, "django_zero/_version.py"), version_ns)
except EnvironmentError:
    version = "dev"
else:
    version = version_ns.get("__version__", "dev")

setup(
    author="Romain Dorgueil",
    author_email="romain@dorgueil.net",
    description="Zero-configuration django projects.",
    license="Apache License, Version 2.0",
    name="django_zero",
    version=version,
    long_description=long_description,
    classifiers=classifiers,
    packages=find_packages(exclude=["ez_setup", "example", "test"]),
    include_package_data=True,
    install_requires=[
        "brotli ~= 1.0.4",
        "django ~= 3.0, < 3.1",
        "django-allauth ~= 0.41.0",
        "django-includes ~= 0.3.1",
        "jinja2 ~= 2.10",
        "mondrian ~= 0.8",
        "whitenoise ~= 5.0.1",
    ],
    extras_require={
        "celery": ["celery ~= 4.4.0", "django_celery_beat ~= 1.5.0", "django_celery_results ~= 1.1.2"],
        "channels": ["channels ~= 2.4.0", "daphne ~= 2.4.1"],
        "dev": [
            "cookiecutter ~= 1.7",
            "coverage ~= 4.5",
            "django-extensions ~= 2.2",
            "django_debug_toolbar ~= 2.1",
            "honcho ~= 1.0",
            "isort",
            "medikit ~= 0.7",
            "pyquery ~= 1.4",
            "pytest ~= 4.6",
            "pytest-cov ~= 2.7",
            "pytest-django ~= 3.7",
            "werkzeug ~= 0.16",
        ],
        "prod": ["gunicorn ~= 20.0.0"],
    },
    entry_points={"console_scripts": ["django-zero = django_zero.commands:main"]},
    url="https://github.com/hartym/django-zero",
    download_url="https://github.com/hartym/django-zero/archive/{version}.tar.gz".format(version=version),
)
